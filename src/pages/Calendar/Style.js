import { css } from "@emotion/react";

export const SLayout = (isShow) => css`
    position: relative;
    z-index: 1;
    left: 0;
    flex-grow: 1;
    height: 100%;
    background-color: #eaeaea;
`;